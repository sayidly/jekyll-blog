rvm:
  - 1.29.7

install:
  - gem install bundler jekyll

script:
  - bundle install
  - jekyll build

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GH_TOKENS
  on:
    branch: master
  local_dir: _site